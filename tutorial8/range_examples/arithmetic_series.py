numbers = list(range(10, 100, 10))  # Generates a list of values 10, 20, ..., which are smaller than 100
print(numbers)

numbers = list(range(25, 2, -2))  # Generate a list of values 25, 23, ..., which are greater than 2
print(numbers)
